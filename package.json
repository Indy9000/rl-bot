{
  "name": "rl-bot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",

  "dependencies": {
    "core-js": "^2.4.0",
    "fable-core": "0.6.8",
    "requirejs": "^2.2.0"
  },
  "engines": {
    "fable": ">=0.2.11"
  },
  "scripts": {
    "build": "fable",
    "watch": "fable -w --target watch"
  }  
}
